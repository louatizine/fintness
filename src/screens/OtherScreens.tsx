import { StyleSheet, Text, View } from 'react-native';
import { colors, spacing } from '../theme';
import { atlasConfigured } from '../services/mongoApi';
export function HistoryScreen() { return <Placeholder title="History" subtitle="Your completed sessions will appear here." />; }
export function ProgressScreen() { return <Placeholder title="Progress" subtitle="Volume, streaks and personal records will appear here." />; }
export function SettingsScreen() { return <View style={styles.screen}><Text style={styles.eyebrow}>ACCOUNT</Text><Text style={styles.title}>Settings</Text><View style={styles.row}><Text style={styles.rowLabel}>Weight unit</Text><Text style={styles.value}>kg</Text></View><View style={styles.row}><Text style={styles.rowLabel}>Atlas connection</Text><Text style={[styles.value, { color: atlasConfigured ? colors.accent : colors.danger }]}>{atlasConfigured ? 'Ready' : 'Not configured'}</Text></View></View>; }
function Placeholder({ title, subtitle }: { title: string; subtitle: string }) { return <View style={styles.screen}><Text style={styles.eyebrow}>FINTNESS</Text><Text style={styles.title}>{title}</Text><Text style={styles.subtitle}>{subtitle}</Text></View>; }
const styles = StyleSheet.create({ screen: { flex: 1, backgroundColor: colors.background, padding: spacing.lg }, eyebrow: { color: colors.accent, fontSize: 11, fontWeight: '800', letterSpacing: 1 }, title: { color: colors.text, fontSize: 30, fontWeight: '900', marginTop: spacing.sm }, subtitle: { color: colors.muted, fontSize: 16, lineHeight: 24, marginTop: spacing.md }, row: { borderBottomColor: colors.border, borderBottomWidth: 1, paddingVertical: spacing.md, flexDirection: 'row', justifyContent: 'space-between' }, rowLabel: { color: colors.text, fontSize: 16 }, value: { color: colors.muted, fontWeight: '700' } });